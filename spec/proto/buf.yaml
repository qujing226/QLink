version: v2

modules:
  - path: .

deps:
  - buf.build/protocolbuffers/wellknowntypes
  - buf.build/bufbuild/protovalidate

lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX
  ignore:
    - vendor/
  ignore_only:
    ENUM_ZERO_VALUE_SUFFIX:
      - doirp_v3/v1/core.proto

breaking:
  use:
    - FILE
